<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jims.clinic.dao.PerformDefaultScheduleDao">
    
	<sql id="performDefaultScheduleColumns">
		a.id AS "id",
		a.freq_desc AS "freqDesc",
		a.administration AS "administration",
		a.default_schedule AS "defaultSchedule"
	</sql>
	
	<sql id="performDefaultScheduleJoins">
	</sql>
    
	<select id="get" resultType="PerformDefaultSchedule">
		SELECT 
			<include refid="performDefaultScheduleColumns"/>
		FROM perform_default_schedule a
		<include refid="performDefaultScheduleJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="PerformDefaultSchedule">
		SELECT 
			<include refid="performDefaultScheduleColumns"/>
		FROM perform_default_schedule a
		<include refid="performDefaultScheduleJoins"/>
		<where>
			
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="PerformDefaultSchedule">
		SELECT 
			<include refid="performDefaultScheduleColumns"/>
		FROM perform_default_schedule a
		<include refid="performDefaultScheduleJoins"/>
		<where>
			
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO perform_default_schedule(
			id,
			freq_desc,
			administration,
			default_schedule
		) VALUES (
			#{id},
			#{freqDesc},
			#{administration},
			#{defaultSchedule}
		)
	</insert>
	
	<update id="update">
		UPDATE perform_default_schedule SET 	
			freq_desc = #{freqDesc},
			administration = #{administration},
			default_schedule = #{defaultSchedule}
		WHERE id = #{id}
	</update>
	
	<update id="delete">
		DELETE FROM perform_default_schedule
		WHERE id = #{id}
	</update>
	
</mapper>