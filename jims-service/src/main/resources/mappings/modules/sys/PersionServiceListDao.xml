<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jims.sys.dao.PersionServiceListDao">

    <sql id="persionServiceListColumns">
		a.id AS "id",
		a.persion_id AS "persionId",
		a.service_id AS "serviceId",
		a.flag AS "flag",
		a.service_start_date AS "serviceStartDate",
		a.service_end_date AS "serviceEndDate",
		a.remarks AS "remarks",
		a.update_by AS "updateBy.id",
		a.create_by AS "createBy.id",
		a.update_date AS "updateDate",
		a.del_flag AS "delFlag",
		a.create_date AS "createDate"
	</sql>

    <sql id="persionServiceListJoins">
    </sql>


    <select id="get" resultType="PersionServiceList">
        SELECT
        *
        FROM persion_service_list a

        WHERE a.id = #{id}
</select>
    <select id="findListByFlag" resultType="SysService">
        select se.service_image,se.id,se.service_name from sys_service se,persion_service_list pp where pp.persion_id=#{persionId}
        and pp.service_id=se.id

	</select>

    <insert id="insert">
		INSERT INTO persion_service_list(
			id,
			persion_id,
			service_id,
			flag,
			service_start_date,
			service_end_date,
			remarks,
			update_by,
			create_by,
			update_date,
			del_flag,
			create_date
		) VALUES (
			#{id},
			#{persionId},
			#{serviceId},
			#{flag},
			#{serviceStartDate},
			#{serviceEndDate},
			#{remarks},
			#{updateBy},
			#{createBy},
			#{updateDate},
			#{delFlag},
			#{createDate}
		)
	</insert>

    <update id="update">
		UPDATE persion_service_list SET
			persion_id = #{persionId},
			service_id = #{serviceId},
			flag = #{flag},
			service_start_date = #{serviceStartDate},
			service_end_date = #{serviceEndDate},
			remarks = #{remarks},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			del_flag=#{delFlag}
		WHERE id = #{id}
	</update>

    <update id="delete">
		UPDATE persion_service_list SET
			del_flag ='1'
		WHERE id = #{id}
	</update>
</mapper>